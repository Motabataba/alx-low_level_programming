#include "main.h"

/**
 * print_line - check code
 * @n: argument
 *
 * Return: nothing
 */
void print_line(int n)
{
int i;
if (n >= 0)
{
for (i = 0; i < n; i++)
{
if (n != 0)
{
_putchar('-');
}
else
{
_putchar('\n');
}
}
}
else
{
for (i = n; i < 0; i--)
{
_putchar('-');
}
}
}
