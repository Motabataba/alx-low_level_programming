#include "main.h"

/**
 * print_number - check code
 * @n: argument
 *
 * Return: Nothing
 */
void print_number(int n)
{
unsigned int k = n;
if (n < 0)
{
_putchar('-');
k = -k;
}

if (k > 9)
{
_putchar(k / 10);
}

_putchar((k % 10) + '0');

}
