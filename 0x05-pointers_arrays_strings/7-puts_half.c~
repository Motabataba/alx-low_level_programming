#include "main.h"

/**
 * puts_half - chec code
 *@str: argument
 *
 * Return: nothing
 */

void puts_half(char *str)
{

int i = 0;
int n;
while (str[i] != '\0')
{
i++;
}

if ((i % 2) == 0)
n = (i - 1) / 2;
else
n = i / 2;

while ((n + 1) <= i)
{
  _putchar(str[(n + 1)]);
n++;
}
_putchar('\n');
}
